version: v1.0
name: Pipeline 2
blocks:
  - name: Deployment
    task:
      jobs:
        - name: Deploy
          commands:
            - echo "job 1"
  - name: Tests on a staging env
    task:
      jobs:
        - name: 'Job #1'
          commands: []
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
promotions:
  - name: Production
    pipeline_file: pipeline_3.yml
