version: "v1.0"
name: Complex pipeline without dependencies
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
    
fail_fast:
  cancel:
    when: "branch != 'master'"

blocks:
  - name: "A"
    dependencies: []
    task:
      jobs:
      - name: "A1"
        commands:
          - "false"
      - name: "A1"
        commands:
          - sleep 10
