version: v1.0
name: Complex pipeline without dependencies
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
global_job_config:
  prologue:
    commands:
      - checkout
blocks:
  - name: A
    task:
      jobs:
        - name: A
          commands:
            - err_counter=0
            - go get github.com/lib/pq || err_counter=$((err_counter+1))
            - go get github.com/lib/pq || err_counter=$((err_counter+1))
            - go get github.com/lib/pq || err_counter=$((err_counter+1))
            - go get github.com/lib/pq || err_counter=$((err_counter+1))
            - go get github.com/lib/pq || err_counter=$((err_counter+1))
            - echo $err_counter
          parallelism: 50
