version: "v1.0"
name: Complex pipeline without dependencies
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

promotions:
  - name: staging
    pipeline_file: staging.yml

blocks:
  - name: "Unit tests 1/3"
    task:
      jobs:
      - name: "RSpec 1/3"
        commands:
          - sleep 3000
  - name: "Unit tests 2/3"
    task:
      jobs:
      - name: "RSpec 2/3"
        commands:
          - sleep 3000
  - name: "Unit tests 3/3"
    task:
      jobs:
      - name: "RSpec 3/3"
        commands:
          - sleep 3000
